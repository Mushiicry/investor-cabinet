# HANDOFF — координационная доска Claude ↔ Codex

**Живой статус-борд.** Не исторический лог (для истории — `CODEX_TRACKER_*.md`, `PATCH_LOG.md`).
Здесь только: кто чем владеет, что открыто прямо сейчас, чего не делать.

**Прямого канала между агентами нет.** Claude и Codex — разные системы. Координация идёт
через этот файл + git. Владелец решений и шлюз между агентами — **Муши (человек)**.

---

## 0. Протокол работы

1. **Читать этот файл первым** в начале сессии.
2. Работать **только в своей зоне** (§2). Чужая зона — исключительно по явной команде владельца.
3. **Одна тема — один владелец.** Не заводить параллельные ветки на ту же задачу
   (уже жгло: solana/signals делались дважды → хаос истории).
4. После работы — **обновить свою секцию** (§4/§5): что закрыто, что открыто.
5. Необратимое (прод-деплой, удаление веток, смена конфигов) — **только с апрувом Муши**.

---

## 1. Текущее состояние системы

| | |
|---|---|
| V2 baseline | ✅ рабочая версия 2 сайта зафиксирована перед большим Patch 3; checkpoint tag: `v2-baseline-pre-patch3-2026-07-21` |
| main | `2f05bf0`+ — стейбл-потоки, покупки на кошельках, рефактор walletLedger |
| main `/exec` | ✅ `success:true` (LIVE), деплой `@54`, URL не менялся |
| wife `/exec` | ✅ `success:true` (LIVE) |
| Тесты | ✅ 24/24 (6 файлов) |
| Lint / Build | ✅ чисто / OK |
| `npm audit --omit=dev` | ✅ 0 vulnerabilities |
| Кошельковые импорты | ✅ Общее ядро `apps-script/walletLedger.gs` (`IC_LEDGER_*`): поиск строки, аудит-строки (Пополнение/Вывод/Обмен/Покупка/Продажа), усреднение входа, создание стейбл-строки, расширение валидаций. Arbitrum и BNB зависят ТОЛЬКО от ядра, не друг от друга |
| Таблица | ✅ ревизия 2026-07-16: 0 ошибок, health сайт = health таблицы (одна модель), листы «Портфель»/«HL_DEBUG» удалены, F&G-обновлятор жив |

---

## 2. Зоны ответственности

| Зона | Владелец | Примечание |
|---|---|---|
| `src/v2/**` (frontend, компоненты, стили) | **Claude** | вся продуктовая логика и UI |
| `apps-script/**` (`.gs`, doGet, импорты) | **Claude** | + `clasp push` / передеплой |
| Архитектура, рефакторинг, сегментация | **Claude** | |
| Диагностика инцидентов, безопасность | **Claude** | креатив и защита — только Claude |
| `docs/**` (tracker, baseline, changelog) | **Codex** | ← кроме этого файла (общий) |
| `test/**`, `test/contracts/**` | **Codex** | поддержка тестов при изменениях |
| Verification / smoke / гигиена веток | **Codex** | по ТЗ, без изобретательства |
| `api/**`, `vercel.json` | **Codex** | менять только по согласованию с Claude |
| Vercel Dashboard, Script Properties, апрувы | **Муши** | доступы только у владельца |

**Codex работает строго по ТЗ.** Никакого креатива, security-инициатив и архитектурных решений.

---

## 3. Анти-паттерны — НЕ ДЕЛАТЬ

1. ❌ **Не возвращать shared-secret guard в Apps Script.** Авторизацию уже держит один слой —
   Vercel serverless proxy (`api/_investorProxy.js`) на Supabase-токене + email владельца.
   Guard дал ноль безопасности и стоил ~2 дней простоя main-аккаунта.
2. ❌ **`git revert` ≠ деплой.** Для `.gs` откат кода без `clasp push` + передеплоя оставляет
   сломанный деплой. Web-app `/exec` версионного деплоя не обновляется от push в HEAD.
3. ❌ **Не «хардить» без модели угроз.** Security ради security ломает рабочее.
4. ❌ **Не вести параллельные ветки на одну тему.** Одна тема — один владелец.
5. ❌ **Не менять Apps Script deployment URL.** Передеплой — только тем же deployment ID.
6. ❌ **Не держать `.gs` незакоммиченными** — `clasp pull` / параллельный агент их затирают.
7. ❌ **`setFormula` только с `;`-разделителями.** Таблица в русской локали: `,` — десятичный
   разделитель, формулы с запятыми дают `#ERROR!` (IFERROR не спасает). Инцидент 2026-07-16:
   каскад #ERROR! по Обзору/Риску. Образец правильных формул — `IC_HL_refreshPortfolioAccounting_`.
8. ❌ **Не вставлять строки в «Расчеты» через `insertRow`** — правее колонки K живут служебные
   блоки (L–O агрегаты, W–X фьючерсный учёт), вставка их сдвигает и ломает. Копировать
   строки-шаблоны — только A..K, не `getLastColumn()`.

---

## 4. Открытые задачи — Codex

- [x] **Stage 0 Risk & Health baseline** — создан manifesto, implementation roadmap,
      read-only Stage 0 audit; runtime-код, API, Apps Script, Sheets и UI не менялись.
      Следующий code scope: `Stage 1A — Speculative Risk cap-control patch`.
- [ ] **Дописать блок SPCXB в конец `CODEX_TRACKER_2026-07-11.md`** — готовый текст выдан
      Муши 2026-07-16. Только вставка в конец файла, один коммит, ничего больше.
- Поддерживать: `docs/` в актуальном состоянии, контракт-тесты при изменениях API,
  гигиену веток (удалять смёрдженные).

**Закрыто недавно:** docs-sync после инцидента, удаление заброшенных hardening-доков,
чистка дублирующих веток.

---

## 5. Открытые задачи — Claude

- [ ] **Patch 3 / Risk & Health implementation** — начинать только от checkpoint
      `v2-baseline-pre-patch3-2026-07-21`. Первый безопасный шаг: убрать futures
      underfunded penalty и перевести active trading sleeve в cap-control без
      изменения API/Sheets.
- [ ] **Симулятор → 6 рычагов** (все грани Health, включая диверсификацию и разбивку
      фьючерсов), чтобы каждую категорию можно было довести до 100 и увидеть путь к сотке.
- [ ] *(опц.)* Коридор и для **Гибкости** — сейчас она всё ещё монотонно растёт от кэша,
      из-за чего «94% кэша» даёт 83 (вырожденный *оптимум* уже убит коридором Резерва).
- [ ] *(опц.)* Авто-детект покупок SPCXB по дельте кошелька (усреднение входа, как Arbitrum).
- [ ] *(P2)* Code-split бандла — сборка предупреждает про >500 kB (echarts уже удалён, не он).
- [ ] *(ждём)* Первый живой прогон Cosmos tx — когда появится транзакция.

**Закрыто недавно:** empty-state везде, Solana RPC failover, Cosmos import self-heal,
сегментация (`accountState` / `v2LabData` / `healthCoreHelpers`), восстановление LIVE после
инцидента, Health Simulator (заглушка → рабочий),
**health-модель: честная диверсификация + коридор резерва (Health=100 стал достижим)**,
Market Psychology (поведенческий движок от живого F&G), полная mobile-адаптация
(8 страниц, гексагон виден, фулскрин BTC-графика), **SPCXB onboard + инцидент
формул RU-локали (анти-паттерны §3.7–8)**.

---

## 6. Открытые задачи — Муши (владелец)

- [x] ~~Удалить мёртвый `APPS_SCRIPT_SHARED_SECRET` в Vercel~~ — **сделано 2026-07-14**.
      Оба эндпоинта после удаления проверены: LIVE.
- Апрувы прод-деплоев и необратимых действий по запросу агентов.

---

## 7. Продуктовый компас

**Risk First. Discipline First. PnL Second.**
Перед любой фичей: улучшает ли она качество решений, контроль риска, видимость портфеля,
дисциплину, ясность? Если на всё «нет» — фичу отклоняем. Мобайл обязателен, не опционален.
